name: star_falling_flutter
description: Star Falling Animation - Flutter + Unity Integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Unity Widget 集成
  flutter_unity_widget: ^2022.2.0
  
  # 其他可能需要的依赖
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # 如果需要添加资源文件
  # assets:
  #   - images/
  #   - assets/
