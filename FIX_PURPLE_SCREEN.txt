# 🔧 紫色屏幕修复指南

## 🎯 问题诊断

**紫色/洋红色背景** = Unity 材质缺失警告

这是因为:
1. ❌ 天空盒未设置
2. ❌ 材质使用了不兼容的着色器
3. ❌ URP 项目使用了 Built-in 着色器

---

## ✅ 已自动修复 (刚刚完成)

我已经更新了代码来自动避免紫色背景:

### 修复 1: 天空盒自动生成
- ✅ `EnvironmentSettings.cs` 现在会自动创建默认天空盒
- ✅ 使用程序化 Skybox/Procedural 着色器
- ✅ 蓝色渐变天空替代紫色

### 修复 2: 道路材质URP兼容
- ✅ `RoadManager.cs` 现在自动检测渲染管线
- ✅ 优先使用 "Universal Render Pipeline/Lit"
- ✅ 回退到 "Standard" 或 "Unlit/Color"

---

## 🚀 立即测试

1. **保存所有脚本**
   ```
   Ctrl + S (或 File > Save All)
   ```

2. **返回 Unity 等待编译**
   - 检查 Console 没有红色错误

3. **播放场景**
   ```
   按播放键 ▶
   ```

4. **检查效果**
   - ✅ 应该显示**蓝色天空**（不是紫色）
   - ✅ 道路应该有颜色（灰色/绿色）
   - ✅ 星星应该正常显示

---

## 🎨 进一步美化（可选）

### 方法1: 手动设置天空盒（推荐）

获得最佳视觉效果:

1. **打开光照设置**
   ```
   Window > Rendering > Lighting
   ```

2. **拖入 Fantasy Skybox**
   - Environment > Skybox Material
   - 从 Project 拖入:
     ```
     Assets/Fantasy Skybox FREE/Cubemaps/Classic/FS000_Day_01.mat
     ```

3. **效果立即生效**
   - ✨ 专业级天空效果
   - ☀️ 动态光照
   - 🌈 真实氛围

---

### 方法2: 使用脚本设置

1. **选中 EnvironmentSettings 对象**
   - 在 Hierarchy 中

2. **启用程序化天空**
   - Inspector > Environment Settings 组件
   - 勾选 "Use Procedural Sky"

3. **调整颜色**
   - Sky Top Color: 蓝色 (0.4, 0.7, 1.0)
   - Sky Middle Color: 浅蓝 (0.8, 0.9, 1.0)
   - Sky Bottom Color: 白色 (1.0, 1.0, 1.0)

---

### 方法3: 使用外部资源（最佳）

1. **打开配置工具**
   ```
   Tools > Star Falling Animation > Configure Assets
   ```

2. **拖入天空盒材质**
   - 从 Fantasy Skybox FREE 文件夹
   - 点击 "应用到场景"

3. **完成！**

---

## 🔍 验证修复成功

### 天空检查清单:
- [ ] 不再显示紫色背景
- [ ] 显示蓝色渐变天空（程序化）
- [ ] 或显示 Fantasy Skybox（如果已设置）

### 道路检查清单:
- [ ] 道路显示深灰色（不是紫色）
- [ ] 绿化带显示绿色
- [ ] 草地显示浅绿色

### 整体效果:
- [ ] 场景色彩正常
- [ ] 无紫色材质错误
- [ ] 动画流畅播放

---

## 🐛 如果还有紫色?

### 检查 1: 渲染管线设置

1. **确认 URP 已安装**
   ```
   Window > Package Manager
   搜索 "Universal RP"
   确保已安装
   ```

2. **检查项目设置**
   ```
   Edit > Project Settings > Graphics
   Scriptable Render Pipeline Settings 应该不为空
   ```

3. **确认摄像机设置**
   ```
   选中 Main Camera
   Rendering > Renderer 选择 URP-HighFidelity 或 URP-Balanced
   ```

---

### 检查 2: 材质着色器

如果特定物体是紫色:

1. **选中紫色物体**

2. **查看 Inspector > Material**
   - 如果显示 "Shader: ERROR" → 着色器丢失

3. **手动设置着色器**
   ```
   点击 Shader 下拉菜单
   选择 "Universal Render Pipeline > Lit"
   ```

---

### 检查 3: 控制台日志

查看 Console 窗口是否有:

```
[EnvironmentSettings] 使用程序化天空盒
[RoadManager] 使用程序化道路生成
```

如果看到这些日志 = 自动修复已生效

---

## 📊 技术细节

### 为什么会出现紫色?

Unity 紫色 (Magenta) 用于标识:
1. **着色器缺失** - 材质引用的着色器不存在
2. **着色器不兼容** - Built-in 着色器用在 URP 项目
3. **材质引用丢失** - 原始材质文件被删除

### 自动修复机制

**EnvironmentSettings.cs**:
```csharp
if (skyboxMaterial != null)
    使用自定义天空盒
else if (useProceduralSky)
    创建程序化天空
else
    创建默认天空盒 ← 新增! 避免紫色
```

**RoadManager.cs**:
```csharp
CreateURPCompatibleMaterial(color):
1. 尝试 "Universal Render Pipeline/Lit" (URP)
2. 回退 "Standard" (Built-in)
3. 最后 "Unlit/Color" (通用)
```

---

## ✅ 完成!

紫色问题应该已经解决。

如果还有问题,请检查:
1. ✅ Unity 版本 (建议 2021.3 LTS+)
2. ✅ URP 包已安装
3. ✅ 项目设置正确
4. ✅ 脚本编译无错误

**祝您使用愉快!** 🎉
